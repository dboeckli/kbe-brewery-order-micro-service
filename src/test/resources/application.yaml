server:
  port: 8081

sfg:
  brewery:
    beer-service-host: http://localhost:8083
  tasting:
    room:
      rate: 500

logging:
  level:
    guru.sfg: debug
    # org.springframework.statemachine: debug

# spring:
#   jpa:
#     show-sql: true
